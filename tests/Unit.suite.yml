class_name: UnitTester
modules:
    enabled:
        - Asserts
        - Db
    config:
        Db:
            dsn: 'mysql:host=localhost;dbname=test'
            user: 'root'
            password: ''
            dump: 'tests/_data/dump.sql'
